# PROMPT — בניית NEON DASH (משחק פלטפורמה ריתמי בדפדפן)

> העתק את כל הטקסט מכאן ולמטה לסוכן הקוד (Claude Code / Cursor / Higgsfield), יחד עם תיקיית `assets/`.

---

בנה משחק פלטפורמה ריתמי דו-ממדי מלא בשם **NEON DASH**, בסגנון של Geometry Dash אבל כיצירה עצמאית: אל תשתמש בשם, בלוגו, במוזיקה, בשמות השלבים או בגרפיקה של המשחק המקורי. המכניקה בלבד היא ההשראה. השתמש בנכסים שבתיקיית `assets/` המצורפת.

## 0. אילוצי מסירה

- **קובץ אחד**: `index.html` שמכיל HTML + CSS + JS. בלי build step, בלי npm, בלי ספריות חיצוניות, בלי CDN.
- Canvas 2D בלבד (לא WebGL, לא DOM לאלמנטים של המשחק).
- עובד גם בפתיחה ישירה מהקובץ (`file://`) וגם משרת. לכן: **נתוני השלבים מוטמעים כאובייקטים ב-JS** בתוך `index.html` (fetch ל-JSON נכשל ב-`file://`). שמור על אותו סכימת JSON של `assets/levels/level-schema.json` כדי שאפשר יהיה לייבא/לייצא שלבים.
- ספרייטים נטענים כ-`Image` מתוך `assets/`. אם טעינה נכשלת — **fallback פרוצדורלי** שמצייר את אותו אובייקט ב-Canvas (צורות גיאומטריות + הצבעים מ-`palette.json`). המשחק חייב להיות משחיק גם בלי שום קובץ נכס ובלי אודיו.
- יעד: 60fps יציב בדסקטופ ובמובייל, ללא gc spikes.
- מסירה בפעם אחת: משחק גמור ומשחיק, לא שלד ולא "שלב 1 מתוך 5".

## 1. פיזיקה — קבועים מדויקים

יחידת רשת: `UNIT = 30px`. כל הערכים בפיקסלים לשנייה.

```js
const PHYS = {
  UNIT: 30,
  TIMESTEP: 1/240,          // סימולציה בצעד קבוע, רינדור מנותק מהפיזיקה
  SPEEDS: { slow: 0.807, normal: 1.0, fast: 1.243, faster: 1.502, fastest: 1.849 },
  BASE_VX: 311.6,           // = 10.386 בלוקים/שנייה במהירות רגילה
  // קובייה: גובה קפיצה 2 בלוקים בכל מהירות, אורך קפיצה ~4.5 בלוקים במהירות רגילה
  CUBE:  { gravity: 2560, jumpV: 555, maxFall: 600, rotDegPerSec: 415 },
  MINI:  { scale: 0.6, gravityMul: 1.25, jumpVMul: 0.86 },
  SHIP:  { gravity: 1550, thrust: -2350, maxUp: 470, maxDown: 470, tiltMaxDeg: 28 },
  BALL:  { gravity: 2000, maxFall: 470, flipCooldown: 0.08 },
  UFO:   { gravity: 2000, impulse: -430, maxFall: 480 },
  WAVE:  { slopeRatio: 1.0, miniSlopeRatio: 1.6 },  // vy = ±vx * ratio, בלי תאוצה
  ROBOT: { gravity: 2560, jumpV: 555, maxChargeSec: 0.30, maxJumpMul: 1.35 },
};
```

כללים:
- **מהירות אופקית קבועה**: `vx = BASE_VX * speedMultiplier`. השחקן תמיד באותו X על המסך (~25% מרוחב הקנבס); העולם נע.
- הכבידה והקפיצה תלויות זמן ולא מהירות — לכן שינוי מהירות מקצר/מאריך את **אורך** הקפיצה ולא את **גובהה**.
- כבידה הפוכה = הכפלת כיוון הכבידה ב-1-. הספרייט מתהפך אנכית.
- Mini: קנה מידה 0.6 לספרייט ול-hitbox, כבידה חזקה יותר וקפיצה נמוכה יותר.

## 2. מצבי משחק (gamemodes)

| מצב | קלט | התנהגות |
|---|---|---|
| Cube | לחיצה/הקשה | קופץ רק כשעל הקרקע. החזקה = קפיצה מיידית בכל נחיתה. סיבוב ויזואלי, נצמד ל-90° בנחיתה. |
| Ship | החזקה | החזקה = תאוצה למעלה, שחרור = נפילה. הטיה ויזואלית לפי `vy`. מוות בפגיעה בתקרה/רצפה בעוצמה? לא — מגע עם בלוק מלמעלה/מלמטה = החלקה, מגע בקוץ = מוות. |
| Ball | לחיצה | כל לחיצה הופכת כבידה מיידית (עם cooldown קצר). ממשיך במהירות אופקית קבועה. |
| UFO | לחיצה | כל לחיצה נותנת דחף כלפי מעלה (קפיצה באוויר), כבידה פועלת תמיד. |
| Wave | החזקה | תנועה באלכסון 45° למעלה בהחזקה, 45° למטה בשחרור. אין אינרציה. משאיר שובל. Mini wave = שיפוע חד יותר. |
| Robot | החזקה | קפיצה משתנה לפי משך ההחזקה (עד 0.30 שניות → עד ×1.35 מגובה קפיצה רגיל). |

מעבר בפורטל משנה מצב מיידית ומאפס `vy` (למעט פורטלי כבידה/גודל/מהירות ששומרים על המצב).

## 3. אובייקטים והתנהגותם

ראה טבלה מלאה ב-`assets/README.md` — מימש את כולם:
`spike`, `spike-mini`, `spike-triple`, `block`, `block-half`, `slope`, `saw`,
`pad-yellow/pink/blue/red`, `orb-yellow/pink/blue/green/red/dash`, `coin`,
`portal-cube/ship/ball/ufo/wave/robot`, `portal-gravity-up/down`, `portal-mini/big`, `portal-dual`, `portal-mirror`,
`speed-slow/normal/fast/faster`, `finish`.

הבחנה קריטית: **פד מופעל אוטומטית במגע. אורב מופעל רק אם השחקן לוחץ בזמן שהוא נוגע בו** (עם buffer של 6 פריימים אחורה וקדימה — לחיצה מעט לפני האורב עדיין תיספר).

## 4. התנגשויות

- כל ה-hitboxes הם AABB, למעט אורבים/מסורים (עיגול).
- **hitbox השחקן קטן ב-15% מהספרייט** (סלחני).
- **hitbox של קוץ = ~40% מגודל הספרייט**, ממורכז בבסיס. זה הכרחי לתחושה נכונה — קוץ ויזואלי שנוגע בפינה לא הורג.
- בלוק: נחיתה מותרת רק כשמגיעים מלמעלה ביחס לכיוון הכבידה. מגע בדופן הצד = מוות.
- מניעת tunneling: בדיקת swept-AABB בין המיקום הקודם לנוכחי בכל צעד פיזיקה.
- Culling: אובייקטים ממוינים לפי `x`; בכל פריים בודקים רק את החלון `[playerX - 2, playerX + 40]` בלוקים (חיפוש בינארי לאינדקס ההתחלה).
- מצב דיבאג `H` מצייר את כל ה-hitboxes.

## 5. פורמט שלב

```json
{
  "meta": { "id":"level-01", "name":"First Light", "difficulty":"easy", "bpm":128,
            "music":"assets/audio/level-01.mp3", "musicOffsetMs":0,
            "startMode":"cube", "startSpeed":1, "lengthBlocks":240 },
  "objects": [ { "t":"spike", "x":14, "y":0 }, { "t":"block", "x":33, "y":0, "rot":0 } ]
}
```

`x` = בלוקים מתחילת השלב, `y` = בלוקים מעל קו הרצפה (`y:0` = יושב על הרצפה).
`assets/levels/level-01.json` הוא שלב הדגמה תקין — השתמש בו כשלב הראשון ובנה עוד שניים באותו פורמט.

## 6. סנכרון למוזיקה

- טען את השיר עם Web Audio API. נגן וקשר את זמן המשחק ל-`audioContext.currentTime` (לא ל-`Date.now()`).
- רשת ביטים: מרחק בלוקים לביט = `(BASE_VX * speed) / UNIT * (60 / bpm)`. כשמייצרים שלבים — הצמד מכשולים לרשת הזו.
- `AnalyserNode` לפעימת רקע: הרקע, קווי הרשת וזוהר האובייקטים פועמים לפי עוצמת הבס. עדין, לא אפילפטי.
- אם אין קובץ מוזיקה — המשחק רץ על שעון פנימי בלי שגיאות.

## 7. לולאת המשחק וחוויית השחקן

זה החלק שקובע אם המשחק מרגיש נכון:

- **מוות מיידי** בפגיעה: פיצוץ חלקיקים (`assets/sprites/fx/`), רעד מסך קצר (~120ms), צליל, מונה ניסיונות עולה.
- **ניסיון חוזר תוך פחות מ-150ms** — R או לחיצה. אפס state, אל תטען מחדש דבר.
- מונה `Attempt N` שצץ ליד השחקן בתחילת כל ניסיון.
- **פס התקדמות** למעלה עם אחוז נוכחי + סימון השיא האישי.
- **מצב אימון (Practice)**: מקש P. השחקן מניח נקודות ביקורת ב-Z ומוחק ב-X; מוות מחזיר לנקודה האחרונה. ההתקדמות במצב אימון נשמרת בנפרד ולא נספרת כהשלמת שלב.
- 3 מטבעות מוסתרים בכל שלב, נשמרים לצמיתות.
- מסך סיום עם אחוז, ניסיונות, מטבעות שנאספו.

## 8. מסכים וממשק

1. **תפריט ראשי** — לוגו (`assets/ui/logo.svg`), רקע חי עם פעימה, כפתורי Play / Levels / Settings.
2. **בחירת שלב** — 3 שלבים עם דרגת קושי, אחוז שיא, מטבעות שנאספו.
3. **משחק** — HUD מינימלי: פס התקדמות, מונה ניסיונות, אחוז.
4. **פאוזה** (ESC) — המשך, ניסיון חדש, מצב אימון, עוצמת קול, יציאה.
5. **הגדרות** — מוזיקה/אפקטים, עוצמת חלקיקים, הצגת hitboxes, מיפוי מקשים, ניגודיות גבוהה.

שליטה: רווח / חץ למעלה / W / קליק שמאלי / מגע = קפיצה-החזקה (כולן שוות). ESC פאוזה, R ניסיון חוזר, P אימון, Z/X נקודות ביקורת, H דיבאג.
מובייל: כל המסך הוא אזור מגע, תמיכה במגע מרובה, ללא זום/גלילה, נעילה לרוחב.

## 9. עיצוב ויזואלי

פלטה מ-`assets/palette.json`: `#140B2E` רקע, `#241148` מוצקים, `#2DE2E6` ציאן, `#FF3864` סכנה, `#F9C80E` זהב, `#9D4EDD` סגול.

- רקע: שתי שכבות parallax (`bg-sunset.svg` איטית, `bg-grid.svg` מהירה יותר) + גוון שמשתנה לפי הקטע בשלב.
- זוהר: `shadowBlur` רק על אובייקטים בולטים (אורבים, פורטלים, השחקן) — לא על הכול, זה הורג ביצועים.
- שובל חלקיקים מאחורי השחקן, צבעו לפי המצב.
- מעבר פורטל: הבזק קצר + שינוי גוון הרקע.
- ללא הבהובים חדים; כבד `prefers-reduced-motion` (מוריד רעידות והבהובים, לא את המשחק).

## 10. ארכיטקטורת קוד

קובץ אחד, אבל מחולק למודולים ברורים עם הערות:

```
CONFIG / PHYS          קבועים
Assets                 טעינת ספרייטים + fallback פרוצדורלי
Input                  מקלדת, עכבר, מגע, buffer של לחיצות
Physics                step(state, dt) טהור — בלי ציור, בלי DOM
Collision              swept AABB + עיגולים
Player                 מכונת מצבים למצבי המשחק
Level                  פרסור, מיון, culling, spatial index
Renderer               שכבות: רקע → עולם → שחקן → חלקיקים → HUD
Audio                  Web Audio, analyser, SFX
Save                   localStorage עם try/catch ונפילה לזיכרון בלבד
Scenes                 menu / select / play / pause / end
Game                   לולאה: accumulator בצעד קבוע + render מנותק
```

הפיזיקה חייבת להיות דטרמיניסטית: אותו קלט באותם פריימים = אותה תוצאה תמיד.

## 11. שמירה

`localStorage` במפתח `neon-dash-save-v1`: שיא אחוז לכל שלב, השלמות, מטבעות, הגדרות, סה"כ ניסיונות.
עטוף ב-try/catch — בסביבות שחוסמות אחסון עובדים מהזיכרון בלי לשבור את המשחק.

## 12. שלושה שלבים

| שלב | קושי | BPM | מלמד |
|---|---|---|---|
| 1 — First Light | קל | 128 | קפיצות, קוצים, בלוקים, פד ואורב צהובים, קטע ספינה קצר |
| 2 — Static Bloom | בינוני | 140 | כדור, UFO, פורטלי כבידה, מסורים, שינויי מהירות |
| 3 — Overdrive | קשה | 160 | Wave, mini, dual קצר, שרשראות אורבים, מעברים צפופים |

כל שלב: 45–75 שניות, מסונכרן לביט, עולה בקושי בהדרגה, 3 מטבעות, ותמיד פתיר — בדוק שכל קפיצה אפשרית לפי קבועי הפיזיקה (מרחק קפיצה מקסימלי ≈ 4.5 בלוקים במהירות רגילה, גובה 2 בלוקים).

## 13. קריטריוני קבלה — עבור על הרשימה לפני שאתה מסיים

- [ ] `index.html` נפתח ישירות בדפדפן ומשחק, גם בלי `assets/` וגם בלי אודיו.
- [ ] 60fps יציב; אין נזילת זיכרון אחרי 100 מיתות.
- [ ] כל 6 מצבי המשחק מיושמים ועוברים דרך פורטלים.
- [ ] פדים פועלים במגע, אורבים רק בלחיצה, עם buffer.
- [ ] hitbox של קוץ סלחני — קפיצה במרחק נכון לעולם לא מתה בטעות.
- [ ] ניסיון חוזר מיידי, מונה ניסיונות, פס התקדמות, אחוז שיא נשמר.
- [ ] מצב אימון עם נקודות ביקורת עובד.
- [ ] 3 שלבים מלאים, כל אחד פתיר מתחילתו לסופו.
- [ ] עובד במגע במובייל, לרוחב, בלי גלילה.
- [ ] אין שום נכס, שם או מוזיקה מ-Geometry Dash.

## 14. אופציונלי (רק אחרי שכל מה שלמעלה עובד)

עורך שלבים פנימי: רשת snap, פלטת אובייקטים, גרירה, מחיקה, בדיקה מיידית, ייצוא/ייבוא JSON באותה סכימה, שמירה ב-localStorage.
